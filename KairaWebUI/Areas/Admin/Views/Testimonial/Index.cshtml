@model KairaWebUI.Helpers.PaginatedList<ResultTestimonialDto>
@{
    ViewData["Title"] = "Testimonials";
}

<div class="page-header">
    <h2 class="page-title">Testimonials</h2>
    <p class="page-subtitle">Manage customer reviews</p>
</div>

<div class="admin-card">
    <div class="card-header-custom">
        <div class="d-flex justify-content-between align-items-center">
            <h3 class="card-title-custom">All Testimonials</h3>
            <a href="/Admin/Testimonial/CreateTestimonial/" class="btn btn-admin btn-admin-primary">
                <i class="bi bi-plus-circle me-2"></i>Create New Testimonial
            </a>
        </div>
    </div>

    <div class="table-responsive">
        <table class="table table-custom">
            <thead>
                <tr>
                    <th style="width: 50px;">#</th>
                    <th>Name Surname</th>
                    <th>Comment</th>
                    <th style="width: 200px;">Actions</th>
                </tr>
            </thead>
            <tbody>
                @{ int count = (Model.PageIndex - 1) * Model.PageSize; }
                @foreach (var item in Model.Items)
                {
                    count++;
                    <tr>
                        <td><strong>@count</strong></td>
                        <td>
                            <i class="bi bi-person-circle me-2 text-primary"></i>
                            <strong>@item.NameSurname</strong>
                        </td>
                        <td>@item.Comment</td>
                        <td>
                            <a href="/Admin/Testimonial/UpdateTestimonial/@item.TestimonialId" class="btn btn-sm btn-admin btn-admin-info me-1">
                                <i class="bi bi-pencil"></i> Edit
                            </a>
                            <a href="/Admin/Testimonial/DeleteTestimonial/@item.TestimonialId" class="btn btn-sm btn-admin btn-admin-danger"
                               onclick="return confirm('Are you sure you want to delete this testimonial?')">
                                <i class="bi bi-trash"></i> Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @if (!Model.Items.Any())
    {
        <div class="text-center py-5">
            <i class="bi bi-inbox" style="font-size: 4rem; color: #ccc;"></i>
            <p class="text-muted mt-3">No testimonials found. Create your first testimonial!</p>
        </div>
    }

    <partial name="_Pagination" model="@Model" />
</div>


